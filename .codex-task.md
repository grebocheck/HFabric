# Task: Phase F (P12 UI tails) + Phase G (P18 distribution) — then stop

`ROADMAP.md` is the spec; read P12 and P18 there first. State: P14, P15, P16,
P6R done (147 backend / 49 frontend tests, coverage gate 63, eslint in CI).
Your sandbox cannot write `.git` — do NOT commit; leave the tree dirty and
stop after Phase G. npm/pip installs are blocked — if a step would need a new
dependency, find a dependency-free way or flag it honestly.

## Phase F — P12: generation-page & arbiter loose ends

- F1 (P12.1). **Learned-profile management.** System tab: a list of learned
  `model_profiles` (model, family, quant, measured RAM/VRAM GB, samples,
  updated) with a per-row Reset and a Reset-all; backend gets
  `GET /api/models/profiles` + `DELETE /api/models/profiles/{model_id}` (+
  `?all=true`) which also clears the sysmon in-memory cache
  (`sysmon.set_learned_profile` has a delete counterpart to add). Stub tests
  for both endpoints. Note in the UI that the LLM has no measured VRAM (its
  load_report is None — capturing subprocess VRAM stays out of scope).
- F2 (P12.2). **Per-job arbiter attribution.** The Queue already receives
  `arbiter.note` events with model/family fields; attach the latest relevant
  note to the matching queued job card (small muted line on the card: e.g.
  "waiting: RAM budget refused (needs ~X GB)" or "swap planned: unload Y").
  Add a keep-warm-eviction reason to the existing warm_evict note if any field
  is missing for attribution. Frontend-only beyond that; extend the existing
  QueuePanel test.
- F3 (P12.3). **Inline previews on the Images tab.** The Images-tab queue
  column shows the swap-plan preview inline (the data already exists via
  `GET /api/jobs/plan` — the System tab uses it); and `ResultPreview` gains a
  quick "Reproduce" + "Vary" action on the current result card (the History
  detail modal already has Edit-in-composer/Variation — reuse that logic, do
  not duplicate it: extract if needed).
- F4 (P12.4). **Memory timeline depth.** The System-tab sparkline gains an
  optional process-RSS series (the `mem.status` events already carry
  process_rss_gb) and hover tooltips showing timestamp + values. Keep it
  dependency-free (SVG/absolute-positioned divs like the existing sparkline).

## Phase G — P18: distribution & run story (G1–G3; P18.4 stays deferred)

- G1 (P18.1). **Prod serving mode.** `HFAB_SERVE_FRONTEND=true` makes FastAPI
  serve `frontend/dist` (StaticFiles mount + SPA fallback for non-/api routes;
  index.html no-cache, hashed assets long-cache). Guard: if dist/ is missing,
  log + 503 page explaining "run npm run build". CI: add a frontend build step
  (`npm run build`) so the build stays green. Stub test: with a tmp dist dir
  monkeypatched, / serves index.html and /api/health still works.
- G2 (P18.2). **One-command launcher.** `run.bat` + `run.sh` gain a `--prod`
  mode: build the frontend if `dist/` is stale (missing or older than newest
  src file), start uvicorn with HFAB_SERVE_FRONTEND=true, wait for
  /api/health, open the browser at the right port. Keep current dev behavior
  as the default path.
- G3 (P18.3). **Writable settings subset.** `data/settings-overrides.json`
  loaded at startup over env defaults for a whitelist ONLY: default_steps,
  default_guidance, default_width/height, keep_warm_models,
  keep_warm_max_models. `GET/PUT /api/settings/overrides` (PUT validates
  against the whitelist + clamps, persists, applies live where trivially safe
  — defaults are read per-job so they apply immediately; keep-warm flags read
  per-swap). Settings drawer gets an editable card for exactly these. Memory-
  safety knobs (min_free_ram_gb etc.) stay env-only — say so in the UI copy.
  Stub tests: GET/PUT roundtrip, non-whitelisted key 422, file persists.

## Rules

- No fakes / silent fallbacks. STUB CI venv has no torch/audio.
- Don't touch `.env` or the voice engine.
- Update ROADMAP P12.1–.4 and P18.1–.3 checkboxes/notes.
- Verify: backend `ruff check app tests` + `pytest -p no:cacheprovider`
  (TEMP workaround as needed); frontend `npx.cmd tsc -b` + `npm.cmd test` +
  `npm.cmd run lint`. Paste outputs honestly.
- Do not commit. Stop after Phase G and summarize.
