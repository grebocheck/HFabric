# --- HFabric backend: Apple Silicon / MPS accelerated deps ---
#
# Install PyTorch first from normal PyPI (no custom CUDA/ROCm index):
#
#   pip install torch torchvision torchaudio
#   pip install --require-hashes -r backend/requirements-mps.lock
#
# Runtime feature gates keep this profile conservative: SDXL image loading is
# enabled on MPS; this file is a resolver input, while CUDA-only/Nunchaku/
# bitsandbytes paths remain hidden until tested on real Macs.

-r requirements-accelerator-common.txt

# Native voice engine / audio stack. ContentVec runs through CPU ONNX Runtime
# on the MPS profile until a tested CoreML/MPS path is added.
onnxruntime>=1.20
